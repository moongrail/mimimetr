DROP TABLE IF EXISTS cats;

CREATE TABLE IF NOT EXISTS cats
(
    id    BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR NOT NULL UNIQUE,
    password VARCHAR NOT NULL,
    image BYTEA,
    rate_user BIGINT,
    rate_world BIGINT,
    CONSTRAINT uq_name_cat UNIQUE (name)
);